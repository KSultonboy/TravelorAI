version: '3.9'

services:
  postgres:
    ports:
      - '5433:5432'

  redis:
    ports:
      - '6380:6379'

  backend:
    command: npx nodemon server.js
    environment:
      NODE_ENV: development
    ports:
      - '4000:4000'
    volumes:
      - ./backend:/app
      - /app/node_modules
